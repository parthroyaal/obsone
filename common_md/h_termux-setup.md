
````bash

pkg update  &&  pkg install wget  openssh iproute2 net-tools  -y 
passwd
sshd

ip addr show
ifconfig

passswd 
termux-change-repo
termux-setup-storage
ssh   u0_482@192.168.238.245 -p 8022
http://192.168.238.245:8000/
termux-setup-storage 
pkg install wget 
wget -O install-nethunter-termux https://offs.ec/2MceZWr 
chmod +x install-nethunter-termux 
./install-nethunter-termux
 ARMhf[3] mimimal
 ls
 nh
 sudo apt update -y && sudo apt install -y wget curl net-tools
 sudo apt install aptitude -y
 sudo aptitude install build-essential
 2
 Y
  sudo  apt install build-essential gcc clang -y
%% sudo  apt install build-essential -y  %%


sudo curl --proto '=https' --tlsv1.2 -sSf https://sh.rustup.rs | sh
sudo ln -s "$HOME/.cargo/bin/cargo" /usr/local/bin/cargo
wget https://dmej8g5cpdyqd.cloudfront.net/downloads/noip-duc_3.1.1.tar.gz
tar xf noip-duc_3.1.1.tar.gz
cd noip-duc_3.1.1

sudo  build --release 


rustup self uninstall
curl --proto '=https' --tlsv1.2 -sSf https://sh.rustup.rs | sh -s -- -y --default-toolchain stable --profile default
sudo mv ~/.cargo /usr/local/cargo
sudo mv ~/.rustup /usr/local/rustup


export RUSTUP_HOME="/usr/local/rustup"
export CARGO_HOME="/usr/local/cargo"
export PATH="/usr/local/cargo/bin:$PATH"

echo 'export RUSTUP_HOME="/usr/local/rustup"' | sudo tee -a /etc/profile
echo 'export CARGO_HOME="/usr/local/cargo"' | sudo tee -a /etc/profile
echo 'export PATH="/usr/local/cargo/bin:$PATH"' | sudo tee -a /etc/profile


echo 'export PATH="/usr/local/cargo/bin:$PATH"' | sudo tee -a /etc/profile
echo 'export PATH="/usr/local/bin:$PATH"' >> ~/.bashrc
source ~/.bashrc

sudo cargo
sudo "$HOME/.cargo/bin/rustup" default stable
curl --proto '=https' --tlsv1.2 -sSf https://sh.rustup.rs | sh
%% apt install build-essential -y %%
%% sudo "$HOME/.cargo/bin/rustup" default stable
%% sudo "$HOME/.cargo/bin/cargo" build --release %% %%
%% . "$HOME/.cargo/env" %%
%% cargo build --release %%
%% %%





sudo cp target/release/noip-duc /usr/local/bin

noip-duc [OPTIONS] --username <USERNAME> --password <PASSWORD>
noip-duc -g myhostname.ddns.net -u username -p password
sudo rm /usr/local/bin/noip-duc
#########

cd /usr/local/src
wget http://www.no-ip.com/client/linux/noip-duc-linux.tar.gz
tar xzf noip-duc-linux.tar.gz
cd noip-2.1.9-1
make
make install
passwd:kali default 


sudo dpkg --add-architecture armhf
sudo dpkg --add-architecture amd64
`sudo apt update`
sudo apt install -f
dpkg --print-foreign-architectures
sudo apt-cache policy x11-xserver-utils


````

````bash
└─$ exit
exit
~ $ history

    5  pkg install wget  -y
    6  wget -O install-nethunter-termux https://offs.ec/2MceZWr
    7  chmod +x install-nethunter-termux
    8  ./install-nethunter-termux
    9  ls
   10  nh
   11  ./install-nethunter-termux
   12  ls
   13  nh
   14  rm -r chroot/*
   15  sud orm -r chroot/*
   16  sudo chroot/
   17  pkg install tsu
   18  clear
   19  rm -r chroot/*
   20  ls
   21  mkdir tmp
   22  mv kali-nethunter-rootfs-nano-armhf.tar.xz  tmp/
   23  ls
   24  clear
   25  ./install-nethunter-termux
   26  lks
   27  ls
   28  rm -r kali-nethunter-rootfs-minimal-armhf.tar.xz
   29  clear
   30  ./install-nethunter-termux
   31  ls
   32  cd tmp/
   33  ls
   34  cd ..
   35  clear
   36  ls
   37  cp kali-nethunter-rootfs-minimal-armhf.tar.xz tmp/
   38  ls
   39  clear
   40  ls
   41  ./install-nethunter-termux
   42  ls
   43  nh
   44  clear
   45  ./install-nethunter-termux
   46  ls
   47  nh
   48  exit
   49  clear
   50  pkg install termux-api
   51  cat >upvol.sh
   52  clear
   53  chmod +x vol*
   54  chmod +x  upvol.sh
   55  clear
   56  ./upvol.sh
   57  cat > monitor_volume.sh
   58  clear
   59  nohup bash monitor_volume.sh &
   60  clear
   61  fg
   62  ls
   63  chmod    +x monitor_volume.sh
   64  nohup bash monitor_volume.sh &
   65  fg
   66  cat nohup.out
   67  ls /dev/input/
   68  clear
   69  ls
   70  mkdir scripts
   71  cd scripts/
   72  vim ~/volume_up_action.sh
   73  clear
   74  vim volume_up_action.sh
   75  vi volume_up_action.sh
   76  ls
   77  whoami
   78  pkg install vim
   79  vi volume_up_action.sh
   80  chmod +x ~/volume_up_action.sh
   81  chmod +x volume_up_action.sh
   82  termux-open volume_up_action.sh
   83  cat ~/volume_log.txt
   84  termux-open volume_up_action.sh
   85  cat ~/volume_log.txt
   86  rm -r ~/volume_log.txt
   87  cat ~/volume_log.txt
   88  termux-open volume_up_action.sh
   89  cat ~/volume_log.txt
   90  pkg install termux-api
   91  termux-toast "hi"
   92  clear
   93  termux-toast "hi"
   94  pkg install oprnssh
   95  pkg install openssh
   96  sshd
   97  clear
   98  passwd
   99  exit
  100  clear
  101  ifconfig
  102  ip addr
  103  pkg install iptoute2
  104  ip addr
  105  nh
  106  sshd
  107  clear
  108  nh
  109  ifconfig
  110  whoami
  111  shd
  112  sshd
  113  exit
  114  clear
  115  hd
  116  clear
  117  nh
  118  history

````

# Jupyter-setup

````bash
1.ipynb  1.py  bash_hist.txt  history.txt  tmp.ipynb

┌──(kali㉿localhost)-[~]
└─$ cat history.txt
    1  clear
    2  sudo apt install -y  vim git wget curl tmux
    3  sudo apt  update -y
    4  sudo apt install -y  vim git wget curl tmux
    5  sudo apt install -y python3 python3-pip
    6  pytho3
    7  python3
    8  clear
    9  python
   10  clear
   11  sudo apt install python-is-python3
   12  sudo  apt install   python3.12-venv -y
   13  python -m venv noteb
   14  source noteb/bin/activate
   15  sudo pip3 install jupyter
   16  sudo pip install jupyter
   17  pip install jupyter
   18  sudo apt install libffi-dev python3-dev build-essential
   19  pip install --upgrade pip setuptools wheel
   20  pip install jupyter
   21  clear
   22  python -m pip install   --no-cache-dir  jupyter
   23  pip install jupyter
   24  clear
   25  jupyter
   26  clear
   27  jupyter  notebook --generate-config
   28  jupyter  notebook password
   29  jupyter notebook          --ip=0.0.0.0
   30  exit
   31  ip addr
   32  sshd
   33  sudo apt unstall openssh -y
   34  sudo apt install openssh -y
   35  sudo apt update
   36  sudo apt install openssh -y
   37  sshd
   38  sudo apt install openssh-server
   39  sshd
   40  ss
   41  sshd
   42  exec bash
   43  sshd
   44  sudo /usr/sbin/sshd
   45  ssh kali@localhost
   46  which sshd
   47  sudo systemctl status sshd
   48  sudo service ssh status
   49  exit
   50  uname -m
   51  getconf LONG_BIT
   52  wget https://github.com/coder/code-server/releases/download/v4.96.2/code-server_4.96.2_armhf.deb
   53  sudo dpkg -i code-server_4.96.2_armhf.deb
   54  sudo apt-get -f install  # Fix dependencies, if needed
   55  ls
   56  code-server
   57  ls
   58  history >bash_hist.txt
   59  ls
   60  pwd
   61  exec bash
   62  source ~/.venv/bin/activate
   63  exec bash
   64  history >bash_hist.txt
   65  clear
   66  exit
   67  clear
   68  code-server --auth none --bind-addr 0.0.0.0:8080  --disable-telemetry
   69  clear
   70  code-server --auth none --bind-addr 0.0.0.0:8080  --disable-telemetry
   71  ls
   72  history
   73  history > history.txt

wsl --install -d Ubuntu-22.04

┌──(kali㉿localhost)-[~]
└─$wsl --install -d Ubuntu-22.04
````
